# 📘 ПОЛНОЕ РУКОВОДСТВО ПО ВЫПОЛНЕНИЮ КУРСОВОЙ РАБОТЫ

## "ER-проектирование схем данных в документоориентированных СУБД"

> **Версия**: 1.0  
> **Дата**: Декабрь 2024  
> **Автор руководства**: GitHub Copilot  

---

# 📋 ОГЛАВЛЕНИЕ

1. [Введение и обзор](#часть-1-введение-и-обзор)
2. [Подготовка окружения](#часть-2-подготовка-окружения)
3. [Работа с MongoDB](#часть-3-работа-с-mongodb)
4. [Написание теоретической части](#часть-4-написание-теоретической-части)
5. [Практическая реализация](#часть-5-практическая-реализация)
6. [Тестирование и анализ](#часть-6-тестирование-и-анализ)
7. [Оформление курсовой](#часть-7-оформление-курсовой)
8. [FAQ и подводные камни](#часть-8-faq-и-подводные-камни)
9. [Python vs JavaScript](#часть-9-python-vs-javascript---анализ)

---

# ЧАСТЬ 1: ВВЕДЕНИЕ И ОБЗОР

## 1.1 Что такое эта курсовая работа?

Эта курсовая работа посвящена изучению **методов проектирования схем данных** в документоориентированных базах данных (на примере MongoDB).

### Основная идея

В реляционных БД (MySQL, PostgreSQL) есть чёткие правила нормализации (1NF, 2NF, 3NF). В документоориентированных БД таких строгих правил нет - есть **4 основных подхода**:

| Метод | Суть | Когда использовать |
|-------|------|-------------------|
| **Embedding** (встраивание) | Вложить данные прямо в документ | Данные читаются вместе |
| **Referencing** (ссылки) | Хранить ID ссылки на другой документ | Данные часто обновляются |
| **Hybrid** (гибридный) | Комбинация первых двух | Смешанные паттерны |
| **Workload-driven** | На основе анализа нагрузки | Оптимизация под конкретные запросы |

### Цель курсовой

1. Изучить и описать все 4 метода
2. Создать таблицу сравнения (⭐ ключевой элемент!)
3. Показать практический пример (система блога)
4. Продемонстрировать применение таблицы к примеру

## 1.2 Структура репозитория

```
coursework-er-mongodb/
├── docs/                          # Документация и материалы
│   ├── 00-structure.md            # План курсовой
│   ├── 01-literature-analysis.md  # Анализ источников
│   ├── 02-mongodb-local-setup.md  # Инструкции установки
│   └── ...                        # Другие разделы
├── examples/                      # Практические примеры
│   └── blog-system/
│       ├── *.json                 # Структуры данных
│       └── queries/               # Скрипты запросов
├── mongodb/                       # Скрипты для БД
│   └── seed-data/                 # Заполнение тестовыми данными
└── tables/                        # Таблицы сравнения
```

## 1.3 Примерный план работы (по дням)

| День | Задача | Время |
|------|--------|-------|
| 1 | Установка MongoDB, изучение основ | 2-3 часа |
| 2-3 | Изучение теории, чтение источников | 4-6 часов |
| 4-5 | Написание раздела 1 (теория методов) | 6-8 часов |
| 6-7 | Практика: запуск примеров, эксперименты | 4-6 часов |
| 8-9 | Написание раздела 2 (примеры) | 6-8 часов |
| 10 | Оформление, проверка, финал | 3-4 часа |

**Итого**: ~30-40 часов работы

---

# ЧАСТЬ 2: ПОДГОТОВКА ОКРУЖЕНИЯ

## 2.1 Установка MongoDB Community Edition

### Почему именно Community Edition?

- **Бесплатно** - не требует регистрации и кредитной карты
- **Локально** - работает на вашем компьютере
- **Полный контроль** - вы владеете данными
- **Неограниченно** - нет лимитов на размер БД

### Шаг за шагом: Windows

```bash
# 1. Скачайте установщик
# Откройте браузер и перейдите на:
# https://www.mongodb.com/try/download/community

# 2. Выберите:
#    - Platform: Windows
#    - Package: MSI
#    - Version: 7.0 (или новее)

# 3. Запустите скачанный .msi файл

# 4. В процессе установки:
#    - Выберите "Complete" (полная установка)
#    - ✅ ОБЯЗАТЕЛЬНО отметьте "Install MongoDB as a Service"
#    - ✅ ОБЯЗАТЕЛЬНО отметьте "Install MongoDB Compass" (GUI)

# 5. После установки откройте командную строку (cmd) или PowerShell:
mongosh

# 6. Если видите приглашение "test>" - всё работает!
```

### Шаг за шагом: macOS

```bash
# 1. Убедитесь, что установлен Homebrew
# Если нет - установите с https://brew.sh
brew --version

# 2. Добавьте репозиторий MongoDB
brew tap mongodb/brew

# 3. Установите MongoDB
brew install mongodb-community

# 4. Запустите сервис MongoDB
brew services start mongodb-community

# 5. Проверьте что работает
mongosh

# 6. Если видите "test>" - успех!
```

### Шаг за шагом: Ubuntu/Debian Linux

```bash
# 1. Установите необходимые пакеты
sudo apt-get update
sudo apt-get install -y gnupg curl

# 2. Импортируйте публичный ключ MongoDB
curl -fsSL https://www.mongodb.org/static/pgp/server-7.0.asc | \
   sudo gpg -o /usr/share/keyrings/mongodb-server-7.0.gpg \
   --dearmor

# 3. Добавьте репозиторий MongoDB
echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-7.0.gpg ] https://repo.mongodb.org/apt/ubuntu jammy/mongodb-org/7.0 multiverse" | \
   sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list

# 4. Обновите пакеты и установите MongoDB
sudo apt-get update
sudo apt-get install -y mongodb-org

# 5. Запустите MongoDB
sudo systemctl start mongod

# 6. Включите автозапуск
sudo systemctl enable mongod

# 7. Проверьте статус
sudo systemctl status mongod

# 8. Подключитесь к MongoDB
mongosh
```

## 2.2 Проверка установки

После установки выполните эти команды для проверки:

```bash
# Подключиться к MongoDB
mongosh

# Вы должны увидеть что-то вроде:
# Current Mongosh Log ID: 6571234567890
# Connecting to: mongodb://127.0.0.1:27017
# Using MongoDB: 7.0.x
# test>
```

Если видите `test>` - MongoDB работает!

### Базовые команды для проверки

```javascript
// В консоли mongosh:

// Показать все базы данных
show dbs

// Создать/переключиться на базу данных
use test_db

// Вставить тестовый документ
db.test.insertOne({ message: "Hello, MongoDB!" })

// Найти документ
db.test.findOne()

// Удалить тестовую коллекцию
db.test.drop()

// Выйти из mongosh
exit
```

## 2.3 Подводные камни при установке

### Проблема 1: MongoDB не запускается (Windows)

**Симптом**: Команда `mongosh` выдаёт ошибку "connect ECONNREFUSED"

**Решение**:
```bash
# Откройте "Службы" (services.msc)
# Найдите "MongoDB Server"
# Убедитесь что статус "Выполняется"
# Если нет - нажмите "Запустить"
```

### Проблема 2: Порт занят

**Симптом**: "Address already in use: 27017"

**Решение**:
```bash
# Windows
netstat -ano | findstr 27017
# Найдите PID и завершите процесс

# Linux/macOS
sudo lsof -i :27017
# или
sudo kill $(sudo lsof -t -i:27017)
```

### Проблема 3: Нет прав доступа (Linux)

**Симптом**: "Permission denied" при запуске mongod

**Решение**:
```bash
# Создайте директорию для данных с правильными правами
sudo mkdir -p /var/lib/mongodb
sudo chown -R mongodb:mongodb /var/lib/mongodb
sudo chmod -R 755 /var/lib/mongodb
```

## 2.4 Установка дополнительных инструментов

### MongoDB Compass (GUI)

MongoDB Compass - это графический интерфейс для работы с MongoDB.

**Установка**:
1. Скачайте с https://www.mongodb.com/products/compass
2. Установите как обычное приложение
3. Подключитесь к `mongodb://localhost:27017`

**Зачем нужен**:
- Визуальный просмотр данных
- Построение запросов без кода
- Анализ производительности
- Удобно для демонстрации преподавателю

### VS Code с расширениями

```bash
# Рекомендуемые расширения:
# 1. MongoDB for VS Code - работа с MongoDB прямо в редакторе
# 2. JSON Tools - форматирование JSON
# 3. Markdown All in One - для документации
```

---

# ЧАСТЬ 3: РАБОТА С MONGODB

## 3.1 Основные концепции

### Терминология: SQL vs MongoDB

| SQL (Реляционные БД) | MongoDB | Описание |
|---------------------|---------|----------|
| Database | Database | База данных |
| Table | Collection | Коллекция документов |
| Row | Document | Один документ (запись) |
| Column | Field | Поле документа |
| Primary Key | _id | Уникальный идентификатор |
| JOIN | $lookup | Объединение данных |
| Index | Index | Индекс для ускорения |

### Структура документа

```javascript
// Документ в MongoDB - это JSON-подобная структура (BSON)
{
  "_id": ObjectId("507f1f77bcf86cd799439011"),  // Уникальный ID
  "username": "alice",                           // Строка
  "age": 25,                                     // Число
  "email": "alice@example.com",                  // Строка
  "created_at": ISODate("2024-01-15T10:00:00Z"), // Дата
  "tags": ["developer", "blogger"],              // Массив
  "profile": {                                   // Вложенный объект
    "bio": "Разработчик",
    "avatar": "/images/alice.png"
  }
}
```

## 3.2 Запуск примеров из репозитория

### Шаг 1: Клонирование репозитория

```bash
# Если ещё не клонировали
git clone https://github.com/YOUR_USERNAME/coursework-er-mongodb.git
cd coursework-er-mongodb
```

### Шаг 2: Заполнение базы тестовыми данными

```bash
# Запустите скрипт заполнения для метода Embedding
mongosh < mongodb/seed-data/seed-blog-embedding.js

# Или для метода Referencing
mongosh < mongodb/seed-data/seed-blog-referencing.js

# Или для Hybrid метода
mongosh < mongodb/seed-data/seed-blog-hybrid.js
```

### Шаг 3: Запуск запросов

```bash
# Запустить запросы для Embedding
mongosh < examples/blog-system/queries/embedding-queries.js

# Запустить запросы для Referencing
mongosh < examples/blog-system/queries/referencing-queries.js

# Запустить запросы для Hybrid
mongosh < examples/blog-system/queries/hybrid-queries.js

# Запустить тесты производительности
mongosh < examples/blog-system/queries/performance-tests.js
```

### Что происходит при запуске?

Каждый скрипт:
1. Создаёт/выбирает базу данных
2. Очищает существующие коллекции
3. Вставляет тестовые данные
4. Выполняет несколько запросов
5. Выводит результаты в консоль

## 3.3 Интерактивная работа с MongoDB

Вместо запуска скриптов можно работать интерактивно:

```bash
# Запустите mongosh
mongosh

# Теперь вы можете вводить команды вручную:
```

```javascript
// Выбрать базу данных
use blog_embedding

// Посмотреть коллекции
show collections

// Вставить документ
db.users.insertOne({
  _id: 1,
  username: "alice",
  email: "alice@example.com"
})

// Найти все документы
db.users.find()

// Найти конкретный документ
db.users.findOne({ username: "alice" })

// Обновить документ
db.users.updateOne(
  { username: "alice" },
  { $set: { email: "new_email@example.com" } }
)

// Удалить документ
db.users.deleteOne({ username: "alice" })

// Выйти
exit
```

## 3.4 Понимание структур данных

### Метод 1: Embedding (Встраивание)

**Идея**: Все связанные данные хранятся в одном документе.

```javascript
// ОДИН документ содержит ВСЁ:
// - информацию о пользователе
// - все его посты
// - все комментарии к постам
{
  "_id": 1,
  "username": "alice",
  "email": "alice@example.com",
  "posts": [                          // Посты ВНУТРИ пользователя
    {
      "_id": 101,
      "title": "Мой первый пост",
      "content": "...",
      "comments": [                   // Комментарии ВНУТРИ поста
        {
          "_id": 1001,
          "author": { "_id": 2, "username": "bob" },
          "text": "Отличная статья!"
        }
      ]
    }
  ]
}
```

**Плюсы**:
- Один запрос = все данные
- Быстрое чтение
- Атомарные обновления

**Минусы**:
- Лимит 16MB на документ
- Дублирование данных
- Сложное обновление вложенных элементов

### Метод 2: Referencing (Ссылки)

**Идея**: Каждая сущность в отдельной коллекции, связь через ID.

```javascript
// Коллекция users
{ "_id": 1, "username": "alice", "email": "alice@example.com" }

// Коллекция posts (ссылка на автора через author_id)
{ "_id": 101, "author_id": 1, "title": "Мой первый пост", "content": "..." }

// Коллекция comments (ссылки на пост и автора)
{ "_id": 1001, "post_id": 101, "author_id": 2, "text": "Отличная статья!" }
```

**Плюсы**:
- Нет дублирования
- Легко обновлять отдельные сущности
- Нет ограничений на количество связей

**Минусы**:
- Нужны JOIN ($lookup) для получения связанных данных
- Медленнее при чтении
- Несколько запросов

### Метод 3: Hybrid (Гибридный)

**Идея**: Комбинация встраивания и ссылок.

```javascript
// Коллекция posts
{
  "_id": 101,
  "author": {                    // ВСТРОЕН автор (для быстрого отображения)
    "_id": 1,
    "username": "alice"
  },
  "title": "Мой первый пост",
  "content": "...",
  "comments_count": 5,           // ДЕНОРМАЛИЗОВАННЫЙ счётчик
  "recent_comments": [           // ПОСЛЕДНИЕ комментарии встроены
    { "_id": 1005, "author": { "username": "bob" }, "text": "..." }
  ]
}

// Коллекция comments (отдельно для полного списка)
{ "_id": 1001, "post_id": 101, "author_id": 2, "text": "..." }
```

**Плюсы**:
- Быстрое чтение основных данных
- Гибкость в запросах
- Баланс между методами

**Минусы**:
- Сложность поддержки консистентности
- Нужно синхронизировать данные
- Более сложная логика

### Метод 4: Workload-driven (На основе нагрузки)

**Идея**: Структура оптимизирована под конкретные паттерны запросов.

Этот метод требует:
1. Анализа частоты запросов
2. Определения приоритетов (READ vs WRITE)
3. Оптимизации под наиболее частые операции

---

# ЧАСТЬ 4: НАПИСАНИЕ ТЕОРЕТИЧЕСКОЙ ЧАСТИ

## 4.1 Структура раздела 1

Раздел 1 курсовой должен содержать:

### 1.1 Способ 1: Прямое встраивание (Embedding)

**Что писать**:
- Определение метода
- Когда использовать (use cases)
- Преимущества и недостатки
- Пример JSON структуры
- Пример запроса MongoDB

**Источники для цитирования**:
- MongoDB Official Documentation [17]
- IEEE 2022 "Conversion of ER Model to NoSQL" [8]

**Пример текста**:
```
Прямое встраивание (Direct Embedding) — это метод проектирования схемы данных,
при котором связанные сущности хранятся как вложенные поддокументы внутри
родительского документа [8].

Данный подход рекомендуется использовать когда:
- Связанные данные всегда запрашиваются вместе
- Количество вложенных элементов ограничено
- Вложенные данные редко обновляются независимо

Согласно исследованию IEEE 2024 [12], встраивание обеспечивает ускорение
операций чтения до 98.92% по сравнению с нормализованной структурой.
```

### 1.2 Способ 2: Нормализованные ссылки (Referencing)

**Что писать**:
- Аналогия с внешними ключами в SQL
- Использование $lookup для JOIN
- Когда предпочтительнее ссылки

**Ключевые моменты**:
- ObjectId как тип данных
- Aggregation Pipeline для объединения
- Индексы для оптимизации

### 1.3 Способ 3: Гибридный подход (Hybrid)

**Что писать**:
- Комбинирование встраивания и ссылок
- Денормализация критичных данных
- Стратегии синхронизации

### 1.4 Способ 4: Workload-driven

**Что писать**:
- Анализ паттернов запросов
- Частота обновлений сущностей
- Алгоритмы оптимизации (по [8], [13])

### 1.n ⭐ ТАБЛИЦА СРАВНЕНИЯ (ЦЕНТРАЛЬНЫЙ ЭЛЕМЕНТ!)

Это **главная часть** раздела 1. Создайте подробную таблицу:

| Критерий | Embedding | Referencing | Hybrid | Workload-driven |
|----------|-----------|-------------|--------|-----------------|
| Скорость READ | ⭐⭐⭐⭐⭐ (0.017 сек) | ⭐⭐ (0.240 сек) | ⭐⭐⭐ (0.080 сек) | ⭐⭐⭐⭐⭐ (0.020 сек) |
| Скорость WRITE | ⭐⭐ (0.045 сек) | ⭐⭐⭐⭐ (0.028 сек) | ⭐⭐⭐ (0.035 сек) | ⭐⭐⭐⭐ (0.032 сек) |
| Использование памяти | Высокое | Низкое | Среднее | Оптимальное |
| Консистентность | Высокая | Низкая | Средняя | Высокая |
| Гибкость обновлений | Низкая | Высокая | Средняя | Высокая |
| Сложность запросов | Простые | Сложные | Средние | Средние |
| Масштабируемость | Низкая | Высокая | Средняя | Высокая |

**Источник данных**: IEEE 2024 "Performance Study" [12]

## 4.2 Как правильно цитировать

### Формат цитирования

```
Согласно исследованию [номер], ...
В работе [автор, год] показано, что ...
Как отмечается в [источник], ...
```

### Примеры правильных цитат

```
Смирнов и Толмасов [7] предложили графическую нотацию для моделирования
документных баз данных, где встраивание обозначается символом ⬤⬃⬃⬃,
а ссылки — символом ○───.

Исследование IEEE 2024 [12] показало, что денормализация обеспечивает
улучшение производительности чтения на 98.92% и увеличение пропускной
способности на 96.88%.

Lima и Mello [13] разработали фундаментальный алгоритм workload-driven
проектирования, на который ссылаются все современные работы в данной области.
```

## 4.3 Работа с источниками

### Обязательные источники (минимум)

1. **Дейт К.Дж.** - "Введение в системы баз данных" [2]
   - Для теории ER-модели и нормализации
   - Главы 13-15

2. **Elmasri & Navathe** - "Fundamentals of Database Systems" [3]
   - Для EER модели
   - Chapters 3-4, 9-10

3. **Смирнов М.В.** - "Графическая нотация документных БД" [7]
   - ⚠️ КРИТИЧЕСКИ ВАЖНО для вашей темы!
   - Прямо описывает нотацию для MongoDB

4. **IEEE 2024 Performance Study** [12]
   - Числовые данные для таблицы сравнения
   - Эмпирические результаты

### Где найти источники

```
IEEE Xplore: https://ieeexplore.ieee.org/
ACM Digital Library: https://dl.acm.org/
Springer: https://link.springer.com/
Google Scholar: https://scholar.google.com/
```

---

# ЧАСТЬ 5: ПРАКТИЧЕСКАЯ РЕАЛИЗАЦИЯ

## 5.1 Пример: Система блога

### Сущности и связи

```
┌─────────────┐     1:N      ┌─────────────┐
│   Users     │─────────────▶│    Posts    │
│   (CN1)     │              │             │
└─────────────┘              └─────┬───────┘
       │                           │
       │ 1:N                       │ 1:N
       │                           ▼
       │                    ┌─────────────┐
       └───────────────────▶│  Comments   │
                            │             │
                            └─────────────┘
                                   │
                                   │ M:N
                                   ▼
                            ┌─────────────┐
                            │    Tags     │
                            │             │
                            └─────────────┘
```

### Описание отношений

| Отношение | Тип | Описание |
|-----------|-----|----------|
| Users → Posts | 1:N | Один пользователь может написать много постов |
| Posts → Comments | 1:N | Один пост может иметь много комментариев |
| Users → Comments | 1:N | Один пользователь может написать много комментариев |
| Posts ↔ Tags | M:N | Посты и теги связаны "многие ко многим" |

## 5.2 Реализация методом Embedding

### Шаг 1: Создание структуры данных

```javascript
// Выполните в mongosh:
use blog_embedding

// Создаём пользователя с встроенными постами и комментариями
db.users.insertOne({
  _id: 1,
  username: "alice",
  email: "alice@example.com",
  created_at: new Date(),
  posts: [
    {
      _id: 101,
      title: "Введение в MongoDB",
      content: "MongoDB - это документоориентированная СУБД...",
      created_at: new Date(),
      tags: ["mongodb", "nosql", "database"],
      comments: [
        {
          _id: 1001,
          author: { _id: 2, username: "bob" },
          text: "Отличная статья!",
          created_at: new Date()
        }
      ]
    }
  ]
})
```

### Шаг 2: Запросы для Embedding

```javascript
// Получить пользователя со всеми данными (ОДИН запрос!)
db.users.findOne({ username: "alice" })

// Получить только посты
db.users.findOne(
  { username: "alice" },
  { posts: 1, _id: 0 }
)

// Найти посты по тегу
db.users.find(
  { "posts.tags": "mongodb" },
  { username: 1, "posts.$": 1 }
)

// Добавить комментарий (сложная операция!)
db.users.updateOne(
  { _id: 1, "posts._id": 101 },
  { 
    $push: {
      "posts.$.comments": {
        _id: 1002,
        author: { _id: 3, username: "charlie" },
        text: "Спасибо за информацию!",
        created_at: new Date()
      }
    }
  }
)
```

## 5.3 Реализация методом Referencing

### Шаг 1: Создание коллекций

```javascript
use blog_referencing

// Коллекция пользователей
db.users.insertMany([
  { _id: 1, username: "alice", email: "alice@example.com" },
  { _id: 2, username: "bob", email: "bob@example.com" }
])

// Коллекция постов (ссылка на автора)
db.posts.insertOne({
  _id: 101,
  author_id: 1,  // ССЫЛКА на пользователя
  title: "Введение в MongoDB",
  content: "MongoDB - это документоориентированная СУБД...",
  tag_ids: [1, 2, 3]  // ССЫЛКИ на теги
})

// Коллекция комментариев
db.comments.insertOne({
  _id: 1001,
  post_id: 101,    // ССЫЛКА на пост
  author_id: 2,    // ССЫЛКА на автора
  text: "Отличная статья!"
})

// Коллекция тегов
db.tags.insertMany([
  { _id: 1, name: "mongodb" },
  { _id: 2, name: "nosql" },
  { _id: 3, name: "database" }
])
```

### Шаг 2: Запросы для Referencing (с $lookup)

```javascript
// Получить пост с автором (нужен $lookup!)
db.posts.aggregate([
  { $match: { _id: 101 } },
  { $lookup: {
      from: "users",
      localField: "author_id",
      foreignField: "_id",
      as: "author"
    }
  },
  { $unwind: "$author" }
])

// Получить пост с комментариями
db.posts.aggregate([
  { $match: { _id: 101 } },
  { $lookup: {
      from: "comments",
      localField: "_id",
      foreignField: "post_id",
      as: "comments"
    }
  }
])

// Добавить комментарий (простая операция!)
db.comments.insertOne({
  _id: 1002,
  post_id: 101,
  author_id: 1,
  text: "Новый комментарий"
})
```

## 5.4 Реализация гибридным методом

```javascript
use blog_hybrid

// Пост с встроенным автором и последними комментариями
db.posts.insertOne({
  _id: 101,
  author: {           // ВСТРОЕН для быстрого отображения
    _id: 1,
    username: "alice"
  },
  title: "Введение в MongoDB",
  content: "...",
  tags: ["mongodb", "nosql"],  // ВСТРОЕНЫ как массив строк
  comments_count: 2,           // ДЕНОРМАЛИЗОВАННЫЙ счётчик
  recent_comments: [           // ПОСЛЕДНИЕ комментарии встроены
    {
      _id: 1002,
      author: { username: "bob" },
      text: "Отлично!"
    }
  ]
})

// Отдельная коллекция для ВСЕХ комментариев
db.comments.insertMany([
  { _id: 1001, post_id: 101, author_id: 2, text: "Первый комментарий" },
  { _id: 1002, post_id: 101, author_id: 3, text: "Отлично!" }
])
```

## 5.5 Сравнение запросов

| Операция | Embedding | Referencing | Hybrid |
|----------|-----------|-------------|--------|
| Получить пост с автором | `findOne()` - 1 запрос | `aggregate()` с `$lookup` - сложнее | `findOne()` - 1 запрос |
| Добавить комментарий | `updateOne()` с `$push` - сложно | `insertOne()` - просто | `insertOne()` + `updateOne()` |
| Обновить username | Нужно обновить везде | Обновить 1 документ | Нужно обновить в нескольких местах |
| Получить все комментарии | В 1 запросе (вложены) | `find()` в отдельной коллекции | `find()` в отдельной коллекции |

---

# ЧАСТЬ 6: ТЕСТИРОВАНИЕ И АНАЛИЗ

## 6.1 Запуск тестов производительности

```bash
# Запустите тест производительности
mongosh < examples/blog-system/queries/performance-tests.js
```

### Что делает тест:

1. Создаёт две тестовые базы (embedding и referencing)
2. Вставляет одинаковые данные
3. Выполняет 100 операций чтения для каждого метода
4. Выполняет 100 операций записи для каждого метода
5. Измеряет среднее время
6. Выводит сравнительную таблицу

### Пример вывода:

```
====================================================
  ИТОГИ ТЕСТОВ
====================================================

| Операция | Embedding | Referencing | Победитель |
|----------|-----------|-------------|------------|
| READ     | 0.150 мс  | 2.340 мс    | Embedding  |
| WRITE    | 0.890 мс  | 0.230 мс    | Referencing|

ВЫВОД:
- Для операций ЧТЕНИЯ: Embedding быстрее (один документ)
- Для операций ЗАПИСИ: Referencing быстрее (простая вставка)
```

## 6.2 Анализ результатов

### Интерпретация для курсовой

```
На основе проведённых тестов производительности можно сделать 
следующие выводы:

1. Метод встраивания (Embedding) показал лучшие результаты для 
   операций чтения (в N раз быстрее), что объясняется отсутствием 
   необходимости выполнять JOIN операции.

2. Метод ссылок (Referencing) показал лучшие результаты для 
   операций записи (в M раз быстрее), так как требуется обновить 
   только один документ в отдельной коллекции.

3. Выбор метода зависит от соотношения READ/WRITE операций 
   в конкретном приложении.
```

## 6.3 Создание диаграммы применения (2.3)

Раздел 2.3 - это **применение таблицы из 1.n к вашему примеру**.

### Структура раздела 2.3

```
┌─────────────────────────────────────────────────────────────┐
│                     РАЗДЕЛ 1.n                              │
│              (Таблица сравнения методов)                    │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          │  Стрелка: 1.n → 2.3
                          │  (применение к примеру)
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                     РАЗДЕЛ 2.3                              │
│           (Применение к системе блога)                      │
│                                                             │
│  Анализ требований:                                         │
│  - Посты читаются чаще, чем обновляются → Embedding         │
│  - Комментарии добавляются часто → Referencing              │
│  - Нужен быстрый preview → Hybrid                           │
│                                                             │
│  ВЫВОД: Для системы блога оптимален ГИБРИДНЫЙ подход        │
└─────────────────────────────────────────────────────────────┘
```

### Как обосновать выбор метода

```
На основе таблицы сравнения (раздел 1.n) и анализа требований 
системы блога, для данного примера рекомендуется гибридный подход:

1. Posts ↔ Users: Embedding
   - Автор поста редко меняется
   - Информация об авторе нужна при каждом отображении
   - Таблица 1.n: высокая скорость чтения важнее

2. Posts ↔ Comments: References
   - Комментарии добавляются часто
   - Количество комментариев не ограничено
   - Таблица 1.n: гибкость обновлений важнее

3. Дополнительно: Денормализация счётчиков
   - comments_count для быстрого отображения
   - recent_comments для preview

Данный выбор основан на критериях из таблицы 1.n с учётом 
специфики workload системы блога.
```

---

# ЧАСТЬ 7: ОФОРМЛЕНИЕ КУРСОВОЙ

## 7.1 Структура документа

```
ТИТУЛЬНЫЙ ЛИСТ

СОДЕРЖАНИЕ

ВВЕДЕНИЕ (1-2 страницы)
- Актуальность темы
- Цель и задачи работы
- Объект и предмет исследования

РАЗДЕЛ 1: ER-ПРОЕКТИРОВАНИЕ В ДОКУМЕНТООРИЕНТИРОВАННЫХ СУБД (10-15 страниц)
1.1 Способ 1: Прямое встраивание
1.2 Способ 2: Нормализованные ссылки
1.3 Способ 3: Гибридный подход
1.4 Способ 4: Workload-driven денормализация
1.n ⭐ Таблица сравнения методов (КЛЮЧЕВОЙ ЭЛЕМЕНТ!)

РАЗДЕЛ 2: ПРИМЕРЫ ПРОЕКТИРОВАНИЯ (10-15 страниц)
2.1 Постановка задачи: Система блога
2.2 Реализация 4 методами
2.3 ⭐ Применение таблицы из 1.n (КЛЮЧЕВОЙ ЭЛЕМЕНТ!)

ЗАКЛЮЧЕНИЕ (1-2 страницы)
- Выводы по работе
- Рекомендации

СПИСОК ЛИТЕРАТУРЫ (20-30 источников)

ПРИЛОЖЕНИЯ
- Исходный код скриптов
- Структуры JSON
```

## 7.2 Требования к оформлению

### Общие требования

- Шрифт: Times New Roman, 14 pt
- Межстрочный интервал: 1.5
- Поля: левое 3 см, правое 1.5 см, верх/низ 2 см
- Нумерация страниц: внизу по центру

### Оформление кода

```
Листинг 1 — Структура документа методом Embedding
```

```json
{
  "_id": 1,
  "username": "alice",
  "posts": [...]
}
```

### Оформление таблиц

```
Таблица 1 — Сравнение методов проектирования схем данных

| Критерий        | Embedding | Referencing | Hybrid | Workload |
|-----------------|-----------|-------------|--------|----------|
| Скорость READ   | ...       | ...         | ...    | ...      |
```

### Оформление списка литературы

```
1. Дейт, К. Дж. Введение в системы баз данных / К. Дж. Дейт. — 
   8-е изд. — М. : Вильямс, 2019. — 1328 с.

2. Elmasri, R. Fundamentals of Database Systems / R. Elmasri, 
   S. Navathe. — 7th ed. — Pearson, 2016. — 1272 p.

3. IEEE 2024. Performance Study on Normalization and 
   Denormalization in MES System Databases // IEEE Xplore. — 
   2024. — Document 10762243.
```

## 7.3 Что включить в приложения

### Приложение А: Структуры данных (JSON)

Скопируйте содержимое файлов:
- `examples/blog-system/01-embedding.json`
- `examples/blog-system/02-referencing.json`
- `examples/blog-system/03-hybrid.json`

### Приложение Б: Скрипты MongoDB

Скопируйте код из:
- `examples/blog-system/queries/embedding-queries.js`
- `examples/blog-system/queries/referencing-queries.js`
- `examples/blog-system/queries/hybrid-queries.js`

### Приложение В: Результаты тестов

Скопируйте вывод:
- `examples/blog-system/queries/performance-tests.js`

---

# ЧАСТЬ 8: FAQ И ПОДВОДНЫЕ КАМНИ

## 8.1 Частые вопросы

### Q: Обязательно ли использовать MongoDB?

**A**: Да, MongoDB — наиболее популярная документоориентированная СУБД и имеет отличную документацию. Альтернативы (CouchDB, ArangoDB) менее распространены.

### Q: Можно ли использовать MongoDB Atlas (облако)?

**A**: Можно, но для курсовой рекомендуется локальная установка:
- Нет ограничений по размеру
- Не нужна регистрация
- Работает без интернета
- Полный контроль над данными

### Q: Сколько источников нужно?

**A**: Минимум 20, оптимально 25-30. Из них:
- 3-5 учебников (Дейт, Elmasri, Кузнецов)
- 5-10 научных статей (IEEE, ACM, Springer)
- 3-5 официальная документация MongoDB
- 5-10 дополнительные источники

### Q: Нужно ли программировать?

**A**: Минимально. Основная работа — это:
1. Понимание концепций
2. Запуск готовых скриптов
3. Анализ результатов
4. Написание текста

### Q: Как защищать курсовую?

**A**: Подготовьтесь объяснить:
1. Чем отличаются 4 метода
2. Почему таблица сравнения важна
3. Как выбрать метод для конкретной задачи
4. Результаты ваших экспериментов

## 8.2 Типичные ошибки

### Ошибка 1: Слишком много теории, мало практики

**Проблема**: 20 страниц теории и 2 страницы примеров

**Решение**: Баланс 50/50. Каждый теоретический тезис подкрепляйте примером кода.

### Ошибка 2: Нет собственных экспериментов

**Проблема**: Только цитаты из источников, нет своих данных

**Решение**: Запустите тесты производительности и используйте СВОИ результаты.

### Ошибка 3: Неправильное цитирование

**Проблема**: "MongoDB быстрее PostgreSQL" без ссылки

**Решение**: Каждое утверждение подкрепляйте источником [номер].

### Ошибка 4: Игнорирование раздела 1.n и 2.3

**Проблема**: Нет таблицы сравнения или её применения

**Решение**: Эти разделы — КЛЮЧЕВЫЕ. Уделите им особое внимание!

### Ошибка 5: Копирование без понимания

**Проблема**: Скопировали код, но не понимаете как он работает

**Решение**: Выполните каждую команду вручную в mongosh, поэкспериментируйте.

## 8.3 Чек-лист перед сдачей

```
□ MongoDB установлен и работает
□ Все скрипты запускаются без ошибок
□ Раздел 1 содержит все 4 метода
□ Таблица сравнения (1.n) заполнена с числами
□ Раздел 2 содержит практический пример
□ Диаграмма применения (2.3) показывает связь с 1.n
□ Список литературы содержит 20+ источников
□ Приложения содержат код и структуры данных
□ Текст проверен на ошибки
□ Оформление соответствует требованиям
```

---

# ЧАСТЬ 9: PYTHON vs JAVASCRIPT — АНАЛИЗ

## 9.1 Почему в проекте используется JavaScript?

### Историческая причина

MongoDB Shell (mongosh) использует **JavaScript** как язык запросов. Это не случайность:

1. **JSON** (JavaScript Object Notation) — формат хранения документов в MongoDB
2. **mongosh** — это JavaScript runtime (на базе V8 engine)
3. Документация MongoDB написана с примерами на JavaScript
4. Это стандарт индустрии для работы с MongoDB через shell

### Где используется JavaScript в проекте

```
examples/blog-system/queries/
├── embedding-queries.js      # JavaScript для mongosh
├── referencing-queries.js    # JavaScript для mongosh
├── hybrid-queries.js         # JavaScript для mongosh
└── performance-tests.js      # JavaScript для mongosh

mongodb/seed-data/
├── seed-blog-embedding.js    # JavaScript для mongosh
├── seed-blog-referencing.js  # JavaScript для mongosh
└── seed-blog-hybrid.js       # JavaScript для mongosh
```

### Что делают эти файлы

Это **скрипты для MongoDB Shell**, которые:
- Создают коллекции
- Вставляют тестовые данные
- Выполняют запросы
- Выводят результаты

## 9.2 Можно ли использовать Python?

### ✅ ДА, МОЖНО!

MongoDB имеет **официальный драйвер для Python** — `pymongo`.

### Преимущества Python

1. **Более читаемый синтаксис** для новичков
2. **Знакомый язык** если вы уже знаете Python
3. **Богатая экосистема** (pandas, matplotlib для анализа)
4. **Легче объяснить** преподавателю
5. **Jupyter Notebook** для интерактивной демонстрации

### Сравнение синтаксиса

**JavaScript (mongosh)**:
```javascript
// Вставка документа
db.users.insertOne({
  _id: 1,
  username: "alice",
  email: "alice@example.com"
})

// Поиск документа
db.users.findOne({ username: "alice" })

// Aggregation
db.posts.aggregate([
  { $match: { _id: 101 } },
  { $lookup: {
      from: "users",
      localField: "author_id",
      foreignField: "_id",
      as: "author"
    }
  }
])
```

**Python (pymongo)**:
```python
from pymongo import MongoClient

# Подключение
client = MongoClient('mongodb://localhost:27017/')
db = client['blog_database']

# Вставка документа
db.users.insert_one({
    "_id": 1,
    "username": "alice",
    "email": "alice@example.com"
})

# Поиск документа
db.users.find_one({"username": "alice"})

# Aggregation
db.posts.aggregate([
    {"$match": {"_id": 101}},
    {"$lookup": {
        "from": "users",
        "localField": "author_id",
        "foreignField": "_id",
        "as": "author"
    }}
])
```

### Ключевые отличия

| Аспект | JavaScript (mongosh) | Python (pymongo) |
|--------|---------------------|------------------|
| Запуск | `mongosh < script.js` | `python script.py` |
| Синтаксис | Camelcase: `insertOne` | Snake_case: `insert_one` |
| Объекты | `{ key: value }` | `{"key": "value"}` |
| Методы | `db.collection.method()` | `db.collection.method()` |
| Подключение | Автоматическое | Явное через `MongoClient` |

## 9.3 Как переписать проект на Python

### Шаг 1: Установка pymongo

```bash
# Создайте виртуальное окружение (рекомендуется)
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate

# Установите pymongo
pip install pymongo
```

### Шаг 2: Структура Python проекта

```
coursework-er-mongodb/
├── python/
│   ├── requirements.txt           # Зависимости
│   ├── config.py                  # Настройки подключения
│   ├── embedding_example.py       # Пример Embedding
│   ├── referencing_example.py     # Пример Referencing
│   ├── hybrid_example.py          # Пример Hybrid
│   ├── performance_tests.py       # Тесты производительности
│   └── seed_data.py               # Заполнение данными
├── notebooks/                     # Jupyter notebooks (опционально)
│   ├── 01_embedding.ipynb
│   ├── 02_referencing.ipynb
│   └── 03_comparison.ipynb
└── ...
```

### Шаг 3: Базовый шаблон Python скрипта

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Пример работы с MongoDB методом Embedding
Курсовая работа: ER-проектирование в документоориентированных СУБД
"""

from pymongo import MongoClient
from datetime import datetime
from pprint import pprint

# =====================================================
# ПОДКЛЮЧЕНИЕ К MONGODB
# =====================================================

def get_database():
    """Подключение к локальной MongoDB"""
    client = MongoClient('mongodb://localhost:27017/')
    return client['blog_embedding']

# =====================================================
# СОЗДАНИЕ ДАННЫХ
# =====================================================

def create_sample_data(db):
    """Создание тестовых данных методом Embedding"""
    
    # Очистка существующих данных
    db.users.drop()
    
    # Вставка пользователя с встроенными постами и комментариями
    user_data = {
        "_id": 1,
        "username": "alice",
        "email": "alice@example.com",
        "created_at": datetime.now(),
        "posts": [
            {
                "_id": 101,
                "title": "Введение в MongoDB",
                "content": "MongoDB - документоориентированная СУБД...",
                "created_at": datetime.now(),
                "tags": ["mongodb", "nosql", "database"],
                "comments": [
                    {
                        "_id": 1001,
                        "author": {"_id": 2, "username": "bob"},
                        "text": "Отличная статья!",
                        "created_at": datetime.now()
                    }
                ]
            }
        ]
    }
    
    result = db.users.insert_one(user_data)
    print(f"Вставлен документ с ID: {result.inserted_id}")

# =====================================================
# ЗАПРОСЫ
# =====================================================

def query_examples(db):
    """Примеры запросов для метода Embedding"""
    
    print("\n=== Запрос 1: Получить пользователя со всеми данными ===")
    user = db.users.find_one({"username": "alice"})
    pprint(user)
    
    print("\n=== Запрос 2: Получить только посты ===")
    result = db.users.find_one(
        {"username": "alice"},
        {"posts": 1, "_id": 0}
    )
    pprint(result)
    
    print("\n=== Запрос 3: Найти посты по тегу ===")
    results = list(db.users.find(
        {"posts.tags": "mongodb"},
        {"username": 1, "posts.$": 1}
    ))
    pprint(results)

# =====================================================
# ГЛАВНАЯ ФУНКЦИЯ
# =====================================================

def main():
    print("=" * 50)
    print("  ПРИМЕР EMBEDDING НА PYTHON")
    print("=" * 50)
    
    db = get_database()
    
    print("\n>>> Создание данных...")
    create_sample_data(db)
    
    print("\n>>> Выполнение запросов...")
    query_examples(db)
    
    print("\n>>> Готово!")

if __name__ == "__main__":
    main()
```

## 9.4 Преимущества Python для защиты курсовой

### 1. Легче объяснить код

```python
# Python — читается как английский
user = db.users.find_one({"username": "alice"})

# Легко объяснить:
# "Находим один документ в коллекции users,
#  где поле username равно alice"
```

### 2. Jupyter Notebook для демонстрации

Jupyter позволяет:
- Показывать код и результат рядом
- Добавлять текстовые пояснения
- Визуализировать данные графиками
- Интерактивно изменять параметры

### 3. Визуализация результатов

```python
import matplotlib.pyplot as plt

# Результаты тестов производительности
methods = ['Embedding', 'Referencing', 'Hybrid']
read_times = [0.017, 0.240, 0.080]
write_times = [0.045, 0.028, 0.035]

fig, ax = plt.subplots(1, 2, figsize=(12, 5))

ax[0].bar(methods, read_times, color='blue')
ax[0].set_title('Время чтения (сек)')
ax[0].set_ylabel('Секунды')

ax[1].bar(methods, write_times, color='green')
ax[1].set_title('Время записи (сек)')
ax[1].set_ylabel('Секунды')

plt.tight_layout()
plt.savefig('performance_comparison.png')
plt.show()
```

## 9.5 Вывод: Что выбрать?

### Рекомендация

**Если вы хорошо знаете Python** — переходите на Python!

**Если вам комфортно с JavaScript** — оставьте как есть.

**Для защиты курсовой** Python может быть удобнее:
- Проще объяснить преподавателю
- Jupyter Notebook для демонстрации
- Графики для визуализации

### Следующий шаг

Если вы хотите **переписать проект на Python**, сообщите мне, и я:
1. Создам все Python скрипты
2. Добавлю requirements.txt
3. Создам Jupyter notebooks (опционально)
4. Сохраню оба варианта (JS и Python)

---

# ЗАКЛЮЧЕНИЕ

Это руководство охватывает все аспекты выполнения курсовой работы:

1. ✅ Установка и настройка MongoDB
2. ✅ Понимание 4 методов проектирования
3. ✅ Практическая реализация примеров
4. ✅ Тестирование производительности
5. ✅ Оформление курсовой
6. ✅ Подводные камни и FAQ
7. ✅ Анализ JavaScript vs Python

**Удачи с курсовой работой!** 🎓

---

*Документ создан: Декабрь 2024*  
*Последнее обновление: Декабрь 2024*
